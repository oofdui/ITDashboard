//var User = require("mongoose").model("User");

exports.delete = function(request,response,next){
	//Test by : curl -X DELETE localhost:3000/user/supasate2
	/*
	request.user.remove(
		function(err){
			if(err){
				return next(err);
			}
			else{
				response.json(request.user);
			}
		}
	);
	*/
}